apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: studycoach-staging

resources:
  - ../../base
  - configmap.yaml

patches:
  - path: deployment.yaml

commonLabels:
  env: staging
