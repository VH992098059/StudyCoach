apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: studycoach

resources:
  - ../../base
  - configmap.yaml
  - hpa.yaml

patches:
  - path: deployment.yaml

commonLabels:
  env: production
